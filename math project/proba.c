#include <stdlib.h>
#include <stdio.h>
#include "mathLib.h"
#include "header.h"
/*
	La fonction est en construction 
*/


double normpdf(int x, int mu, int sigma) {
	//double facteur = 1 / (sigma * ...)

	// exponentielle();

	//return 1.0 / (mu - sqrt(2, 2 * PI)) - expo2(10, -exposant(x - sigma, 2) / (2 * exposant(mu, 2)));
	return 0;
}

//double normpdf(int x, int mu) {
//	return normpdf(x, mu, 1);
//}
//
//double normpdf(int x) {
//	return normpdf(x, 0, 1);
//}